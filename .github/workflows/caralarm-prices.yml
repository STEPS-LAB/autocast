# Caralarm prices sync is disabled. Restore schedule + sync job to re-enable.
name: Caralarm prices sync

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Caralarm prices sync is disabled."
          exit 0
