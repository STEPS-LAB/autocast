# Caralarm catalog sync is disabled. Restore schedule + sync job to re-enable.
name: Caralarm catalog sync

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Caralarm catalog sync is disabled."
          exit 0
