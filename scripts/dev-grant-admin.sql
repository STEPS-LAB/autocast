-- Run in your DEV Supabase project only (SQL Editor).
-- 1. Register at http://localhost:3000/register
-- 2. Supabase Dashboard → Authentication → Users → copy your user UUID
-- 3. Replace the placeholder below and execute:

-- UPDATE profiles SET role = 'admin' WHERE id = 'YOUR-USER-UUID-HERE';

-- Optional: verify
-- SELECT id, role, created_at FROM profiles WHERE role = 'admin';
